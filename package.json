{
  "name": "ai-change-tracker",
  "version": "2.1.0",
  "description": "Cross-platform AI change tracking system using shadow git repository",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "ai-tracker": "./dist/cli.js"
  },
  "scripts": {
    "dev": "bun run --watch src/cli.ts",
    "build": "bun build src/cli.ts --outdir dist --target node",
    "build:standalone": "bun build src/cli.ts --compile --outfile ai-tracker.exe",
    "postinstall": "bun run build",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "keywords": [
    "ai",
    "git",
    "tracking",
    "version-control",
    "rollback"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^11.1.0",
    "execa": "^8.0.1",
    "ora": "^7.0.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.5.3",
    "@types/bun": "^1.0.0",
    "@types/node": "^20.10.0",
    "bun-types": "^1.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}